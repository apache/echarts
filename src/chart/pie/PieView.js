define(function (require) {

    var Pie = require('../ChartView').extend({

        type: 'pie',

        init: function (api) {

        },

        render: function (seriesModel, ecModel, api) {

        },

        dispose: function () {}
    });

    return Pie;
});